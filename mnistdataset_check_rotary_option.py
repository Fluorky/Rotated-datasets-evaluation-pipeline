# -*- coding: utf-8 -*-
"""MnistDataset_check_rotary_option.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1OEnTganHKdkbEQUH6mcubR7OefbfcU0s
"""

import tensorflow as tf
import matplotlib.pyplot as plt

(train_images, train_labels), (test_images, test_labels) = tf.keras.datasets.mnist.load_data()

print(f"Training images shape: {train_images.shape}")  # (60000, 28, 28)
print(f"Training labels shape: {train_labels.shape}")  # (60000,)

plt.figure(figsize=(10, 10))

for i in range(9):
    plt.subplot(1, 9, i + 1)
    plt.imshow(train_images[i], cmap='gray')
    plt.title(f"Label: {train_labels[i]}")
    plt.axis('off')

plt.show()

from PIL import Image
import numpy as np

rotated_images = np.array([np.array(Image.fromarray(image).rotate(30)) for image in train_images])

plt.figure(figsize=(10, 10))

for i in range(9):
    plt.subplot(1, 9, i + 1)
    plt.imshow(rotated_images[i], cmap='gray')
    plt.title(f"Label: {train_labels[i]}")
    plt.axis('off')

plt.show()

rotated_images = np.array([np.array(Image.fromarray(image).rotate(45)) for image in train_images])

plt.figure(figsize=(10, 10))

for i in range(9):
    plt.subplot(1, 9, i + 1)
    plt.imshow(rotated_images[i], cmap='gray')
    plt.title(f"Label: {train_labels[i]}")
    plt.axis('off')

plt.show()

rotated_images = np.array([np.array(Image.fromarray(image).rotate(60)) for image in train_images])

plt.figure(figsize=(10, 10))

for i in range(9):
    plt.subplot(1, 9, i + 1)
    plt.imshow(rotated_images[i], cmap='gray')
    plt.title(f"Label: {train_labels[i]}")
    plt.axis('off')

plt.show()



"""Division angles

Not all with the same code, but randomly selected from each range with a precision of one degree:

20-50 degrees

50-90 degrees

90-120 degrees

*Rotate batches by different angles"""